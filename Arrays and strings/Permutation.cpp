#include <iostream.h>
#include <conio.h>
int main()
{
	clrscr();
	string s1,s2;
	cout<<"enter two strings";
	getline(cin,s1);
	getline(cin,s2);
	int l1,l2,*a;
	l1=s1.length();
	l2=s2.length();
	if(l1!=l2)
	{
		cout<<"not permutation";
		return 0;
	}
	else
	{
		a =new int[l1];
		for(int i=0;i<l1;i++)
		{
			c[]
		}
	}
	
	getch();
	return 0;
}
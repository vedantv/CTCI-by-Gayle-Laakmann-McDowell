#include<iostream>
#include<string>
using namespace std;
int main()
{
	string s1,s2;
	cout<<"enter string 1 \n";
	getline(cin,s1);
	cout<<"enter strign 2 \n";
	getline(cin,s2);
	if(s1.length()==s2.length())
    {

    }
    else if(s1.length()==s2.length()+1)
    {

    }
    else if(s1.length()==s2.length()-1)
    {

    }

	s1.length();
	return 0;
}
